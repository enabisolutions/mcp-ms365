# Generated by bin/enabi-build-client.mjs. Re-formatting here serves no
# purpose — the next build:client regenerates it.
src/generated/

# Lockfiles
package-lock.json

# Build artefacts
dist/
node_modules/

# Subagent-driven-development scratch workspace (ledgers, briefs, reports).
# Already gitignored via its own nested .gitignore; excluded here too since
# prettier's CLI doesn't reliably honor a nested .gitignore for this glob.
.superpowers/
